/**
 * Symbol - unique identifier values
 */
export class Symbol {
    toString(): string;
    valueOf(): Symbol;

    static for(key: string): Symbol;
    static keyFor(sym: Symbol): string;

    static iterator: Symbol;
    static toStringTag: Symbol;
}
