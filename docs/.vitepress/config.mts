import { defineConfig } from 'vitepress'

export default defineConfig({
  title: 'Raya',
  description: 'Raya language documentation',
  themeConfig: {
    nav: [{ text: 'Home', link: '/' }],
    sidebar: []
  }
})
